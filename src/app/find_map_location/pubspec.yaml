name: find_map_location
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  latlong2: ^0.9.0
  http: ^1.2.0
  geolocator: ^11.0.0  # Location services for zoom to user position
  permission_handler: ^11.0.0  # iOS/Android location permissions
  shared_preferences: ^2.0.0  # Grid overlay size persistence

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.0  # For mocking HTTP client in tests
  build_runner: ^2.4.0  # For mockito code generation

flutter:
  uses-material-design: true
